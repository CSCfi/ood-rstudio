---
batch_connect:
  template: "basic"
  conn_params:
    - "csrftoken"
script:
  native:
      - '-c'
      - '<%= cores  %>'
      - '-t'
      - '<%= time %>'
      - '--mem=<%= memory %>G'
      - '--gres=nvme:<%= nvme %>'
