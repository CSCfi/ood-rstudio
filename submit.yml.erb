---
batch_connect:
  template: "basic"
  conn_params:
    - "csrftoken"
script:
  native:
      - '-c'
      - '<%= csc_cores  %>'
      - '-t'
      - '<%= csc_time %>'
      - '--mem=<%= csc_memory %>G'
      - '--gres=nvme:<%= csc_nvme %>'
